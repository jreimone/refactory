ROLEMODELMAPPING ExtractRoom FOR <http://www.emftext.org/language/textadventure> 

MoveToNewRoom maps <ExtractXwithReferenceClass>{ 
	 Extract := Item;
	 OrigContainer := Room{
	 	extracts := items;
	 };
	 NewContainer := Room (newName -> name){
	 	moved := items;
	 };
	 ContainerContainer := Adventure{
	 	source := elements;
	 	target := elements;
	 };
}