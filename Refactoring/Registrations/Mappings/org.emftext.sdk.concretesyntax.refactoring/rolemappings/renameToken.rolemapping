ROLEMODELMAPPING FOR <http://www.emftext.org/sdk/concretesyntax> 

"Rename Token" maps <RenameX> { 
	 Nameable := NamedTokenDefinition(name -> name); 
}

"Rename Token Redefinition" maps <RenameX> { 
	 Nameable := TokenRedefinition(name -> name); 
}

"Rename Partial Token" maps <RenameX> { 
	 Nameable := PartialTokenDefinition(name -> name); 
}