REFACTORING ExtractMethod 
FOR <ExtractMethod>

STEPS { 
	create new NewContainer (-> NC) in ContainerContainer;
	assign NC.newName;
	move OrigContainer.extracts  to ->NC;
	create new MovedReference (->MR) in OrigContainer;
	set use of ->NC in ->MR;
} 