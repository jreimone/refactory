## @param $attr - the attribute
## @param $owner - the owner of the attr 
## @param $value - the new value
## @return $setter
## @return $getter
#set( $getter = "${sourceName}.get$targetRoleName ($!range)" )
#set( $iterator = "${sourceName}.iteratorOf$targetRoleName ()" )
#set( $contains = "$targetName == $getter" )
#set( $remover = "${sourceName}.set$targetRoleName (null)" )
#set( $setter = "${sourceName}.set$targetRoleName ($targetName)" )
#if ( $targetRoleName != "" )
#set( $adder = "${sourceName}.addTo$targetRoleName ($targetName)" )
#else
#set( $adder = "${sourceName}.add ($targetName)" )
#end