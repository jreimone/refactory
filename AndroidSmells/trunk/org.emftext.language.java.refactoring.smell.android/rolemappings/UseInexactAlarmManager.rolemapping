ROLEMODELMAPPING FOR <http://www.emftext.org/java>

/*
	Replaces the exact AlarmManager with an inexact.
*/
"Replaces the exact AlarmManager with an inexact" maps <SelectX> { 
	 Selection := references.MethodCall; 
}
