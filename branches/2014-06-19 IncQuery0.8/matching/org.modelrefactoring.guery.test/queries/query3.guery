// connect a red node to a different red node, compute all paths
motif query3
select start,end
where "start.colour=='red'" and "end.colour=='red'"
connected by connection(start>end) find all
where "start!=end"
group by "start"