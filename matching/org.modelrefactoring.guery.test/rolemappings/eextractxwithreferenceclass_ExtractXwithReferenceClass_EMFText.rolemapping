ROLEMODELMAPPING FOR <http://eextractxwithreferenceclass/1.0>

"ExtractXwithReferenceClass for eextractxwithreferenceclass" maps <ExtractXwithReferenceClass> {
	OrigContainer := EOrigContainer {
		extracts := ereferer:EMovedReference -> econtainerref:ENewContainer -> emoved:EExtract;
		referer := ereferer:EMovedReference;
	};
	Extract := EExtract;
	NewContainer := ENewContainer {
		moved := emoved:EExtract;
	};
	ContainerContainer := EContainerContainer {
		source := esource:EOrigContainer;
		target := etarget:ENewContainer;
	};
	MovedReference := EMovedReference {
		containerRef := econtainerref:ENewContainer;
	};
	
	
}

"ExtractXwithReferenceClass for eextractxwithreferenceclass" maps <ExtractXwithReferenceClass> {
	OrigContainer := EOrigContainer {
		extracts := eextracts:EExtract;
		referer := ereferer:EMovedReference;
	};
	Extract := EExtract;
	NewContainer := ENewContainer {
		moved := emoved:EExtract;
	};
	ContainerContainer := EContainerContainer {
		source := esource:EOrigContainer;
		target := etarget:ENewContainer;
	};
	MovedReference := EMovedReference {
		containerRef := econtainerref:ENewContainer;
	};
	
	
}

