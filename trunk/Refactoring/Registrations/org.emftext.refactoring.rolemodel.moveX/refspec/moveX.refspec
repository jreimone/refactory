REFACTORING MoveXSpec 
FOR <MoveX> 

STEPS {
	object sourceContainer := SourceContainer from uptree(INPUT);
	object targetContainer := TargetContainer from uptree(sourceContainer);

	move SourceContainer.sourceContainment to targetContainer;
}