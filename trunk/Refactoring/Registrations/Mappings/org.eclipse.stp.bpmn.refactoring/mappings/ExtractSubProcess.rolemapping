ROLEMODELMAPPING bpmn FOR <http://stp.eclipse.org/bpmn> 

ExtractSubProcess maps <ExtractXwithReferenceClass> {
	Extract := Activity;
	
	NewContainer := SubProcess (newName -> name){
		moved := vertices;
	};
	
	OrigContainer := Pool{
		extracts := vertices;
	};
	
	ContainerContainer := Pool{
		target := vertices;
	}; 
}