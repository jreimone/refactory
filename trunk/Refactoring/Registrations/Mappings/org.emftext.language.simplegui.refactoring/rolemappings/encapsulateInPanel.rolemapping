ROLEMODELMAPPING encapsulateInPanel FOR <http://www.emftext.org/language/simple_gui> 

EncapsulateInPanel maps <ExtractX> {

	Extract := UIElement;
	
	OrigContainer := Container {
		extracts := children;
	};
	
	NewContainer := Panel(newName -> name){
		moved := children;
	};
	
	ContainerContainer := Container {
		source := children;
		target := children;
	};
} 
