### TypeOf[ ${token} ] := de.uni_kassel.fujaba.codegen.sequencer.ExceptionFlow
### TypeOf[ ${comment} ] := java.lang.String
### TypeOf[ ${children} ] := java.lang.String
### TypeOf[ ${default} ] := java.lang.String
### TypeOf[ ${exception} ] := java.lang.String
#set( $sel = $token )
#set( $exceptionText = $sel.ExceptionUMLTransitionGuard.BoolExpr )
##set( $comment = $thenTrans.Comment.Text )
#set( $children = $utility.indent("   ",$!children) )
#set( $exception = $utility.indent("   ",$!exception) )
#if( $comment )
// $comment
#end
try
{
$!children##flowActivity
}
catch ($exceptionText)
{
$!exception##exception case
}
$!default
