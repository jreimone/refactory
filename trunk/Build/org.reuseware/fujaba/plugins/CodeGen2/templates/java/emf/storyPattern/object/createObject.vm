#set( $package = $class.DeclaredInPackage )
#set( $factory = "$utility.upFirstChar($package.Name)Factory" )
#set( $return = $imports.addToImports("${package.fullPackageName}.$factory") )
#set( $createCode = "${factory}.eINSTANCE.create${type}()"  )
#if ( $utility.nullIfEmpty($object.getTypeCastSource()) )
#set ( $createCode = $object.getTypeCastSource() )
#end
#if ( $utility.nullIfEmpty($object.constructionExpression) )
#set ( $createCode = $object.constructionExpression )
#end
$name = $createCode;