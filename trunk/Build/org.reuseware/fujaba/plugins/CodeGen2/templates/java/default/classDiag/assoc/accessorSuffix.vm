#if($role.hasKeyInStereotypes("pluralized"))
#set ($methodSuffix = $utility.pluralize( $utility.upFirstChar($role.AttrName) ) )
#else
#if("class" == $role.AttrName)
#set ($methodSuffix = "$utility.upFirstChar($role.AttrName)_")
#else
#set ($methodSuffix = $utility.upFirstChar($role.AttrName))
#end
#end
