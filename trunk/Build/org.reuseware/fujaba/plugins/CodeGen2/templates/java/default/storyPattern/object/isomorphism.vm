#parse("$lang/default:storyPattern/object/import.vm" )
#set( $secondName = $secondObject.ObjectName )
#set( $secondSet = $secondObject.isSet() )

#if( $secondObject.instanceOf == "EObject" && $object.instanceOf == "EObject")
// disabled isomorphic binding check between EObjects $name and $secondName"
#else
#set( $opComment = "check isomorphic binding between objects $name and $secondName" )
// $opComment
if (!( ##
#if( $secondSet )
!${secondName}.contains ($name)##
#else
!${name}.equals ($secondName)##
#end
#parse("$lang/default:storyPattern/sdmComment.vm")
 )) {throw new Exception("SDMException");};
#end

