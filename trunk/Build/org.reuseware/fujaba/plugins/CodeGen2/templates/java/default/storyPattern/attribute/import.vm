#set( $attr = $elem )
#set( $object = $attr.getRevAttrs() )
#parse("$lang/default:storyPattern/object/import.vm" )
#set( $attrName = $attr.Name )
#set( $attrNameUC = $utility.upFirstChar($attrName))
#set( $attrExpr = $attr.getExpression() )
#set( $attrOp = $attr.getAttrOperationAsText() )
#set( $instanceOf = $attr.InstanceOf)
#set( $final = $instanceOf.isFinal() )
#set( $static = $instanceOf.isStatic() )
#set( $createMethods = 0 )
#set( $createMethods = $instanceOf.CreateAccessMethods )
#set( $constant = ($static && $final) )
#set( $useMethods = ($createMethods==0 || ($createMethods==2 && !$constant)))