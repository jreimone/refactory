#parse("java/default:storyPattern/link/import.vm" )
// check link
#if ( $role.Card.UpperBound==1 )
#set( $source = "$utility.getMethodName($link.getName())($source.getObjectName()$utility.getParams($link.getName()))" )
if (!( $source == $target.getObjectName() )) {throw new Exception("SDMException");};

#else
To-many assocs for code style asWritten are not yet implemented.
See check.vm 
#end