#parse("$lang/default:storyPattern/attribute/import.vm" )
#set( $attrType = $attr.getInstanceOf().getAttrType().getName() )
// assign statement
#if($useMethods)
#if( $attrType == "StringArray" )
${name}.get$attrNameUC ().clear();
${name}.get$attrNameUC ().addAll($attrExpr);
#else
${name}.set$attrNameUC ($attrExpr);
#end
#else
${name}.$attrName = $attrExpr;
#end
