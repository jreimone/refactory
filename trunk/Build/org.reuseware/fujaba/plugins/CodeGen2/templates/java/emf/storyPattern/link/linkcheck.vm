## TODO check if range is correct
#if ( $toOne )
${targetName}.equals (##
#if( $utility.nullIfEmpty($range) || $mparam )
#set( $targetTypeName = $role.Target.Name )
#set( $fullTargetName = "${role.Target.declaredInPackage.fullPackageName}.$targetTypeName" )
#set( $return = $imports.addToImports( $fullTargetName ) )
($targetTypeName) ##
#end
${sourceName}.get$targetRoleName ()#if($utility.nullIfEmpty($range) || $mparam).get($!range$!mparam)#end)##
#elseif ( $indmulti )
$indmulti##
#elseif ( $source.InstanceOf == "Setting" && $source.InstanceOf.DeclaredInPackage == "ecore.EStructuralFeature" && $targetRoleName == "Value")
#set( $return = $imports.addToImports( "org.eclipse.emf.common.util.EList" ) )
#if ( $multi )
${sourceName}.getEStructuralFeature().isMany() ? ((EList<Object>)${sourceName}.get(true)).indexOf($targetName) == $mparam : ${sourceName}.get(true).equals($targetName)##
#else
${sourceName}.getEStructuralFeature().isMany() ? ((EList<Object>)${sourceName}.get(true)).contains($targetName) : ${sourceName}.get(true).equals($targetName)##
#end
#else
#if ( $multi )
${sourceName}.get$targetRoleName ().indexOf ($targetName) == $mparam##
#else
${sourceName}.get$targetRoleName ().contains ($targetName)##
#end
#end
