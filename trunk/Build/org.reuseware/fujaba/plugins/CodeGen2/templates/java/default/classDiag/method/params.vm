#set( $firstParam = true )
#foreach( $param in $params)
#if( !$firstParam ), #else
#set( $firstParam = false )#end
#set( $classToImport = $param.ParamType )
#if ( ${classToImport.InheritedCodeStyle} )#set( $importStyle = "$lang/${classToImport.InheritedCodeStyle.Name}:" )
#else#set( $importStyle = "$lang/default:" )#end
#parse("$!{importStyle}classDiag/class/addClassImport.vm" )
#set( $paramType = $utility.getTypeAsString($param.ParamType) )
#if( $param.ParamType == "StringArray")
#set( $paramType = "EList<String>" )
#else
#set( $package = $param.ParamType.DeclaredInPackage )
#if( $package && $param.ParamType != "String")
#set( $return = $imports.addToImports("${package.fullPackageName}.$paramType") )
#end
#end
#set( $paramName = $param.Name )
#if ( $param.hasKeyInStereotypes("multiple"))
EList<${paramType}> $paramName ##
#else
$paramType $paramName ##
#end
#end